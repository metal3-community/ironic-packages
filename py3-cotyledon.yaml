package:
  name: py3-cotyledon
  description: Cotyledon provides a framework for defining long-running services.
  version: 1.7.3
  epoch: 1
  copyright:
    - license: Apache-2.0
  dependencies:
    provider-priority: 0
    runtime:
      - py3-setproctitle

environment:
  contents:
    packages:
      - busybox
      - py3-build
      - py3-installer
      - py3-setproctitle
      - python3

pipeline:
  - uses: git-checkout
    with:
      repository: https://github.com/sileht/cotyledon
      tag: ${{package.version}}
      expected-commit: 4d0589aaa3f41542b589ea645e39a9da93d37d71
  - uses: python/build-wheel
  - uses: strip

var-transforms:
  - from: ${{package.name}}
    match: ^py3-(.+)$
    replace: $1
    to: import

test:
  pipeline:
    - uses: python/import
      with:
        imports: |
          import ${{vars.import}}
